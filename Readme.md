``` 
Habr parser

positional arguments:
  threads     Number of threads to be run
  out_dir     Directory to download habr images

optional arguments:
  -h, --help  show this help message and exit
  -n N        Number of articles to be processed
  ```